version: '3.8'

services:
  web:
    container_name: pointvente-app-api
    build: .
    restart: unless-stopped
    environment:
      - SERVER_PORT=8009
      - SPRING_DATASOURCE_URL=jdbc:postgresql://pgsql.prod:5432/pointvente_db
      - SPRING_DATASOURCE_USERNAME=postgres
      - SPRING_DATASOURCE_PASSWORD=89RQ7kaF7770Qy4343_t8hcjCfsqsKs
    # Décommentez si vous souhaitez mapper le port directement sur l'hôte :
    # ports:
    #   - "8009:8009"
    networks:
      - apps.prod

networks:
  apps.prod:
    external: true
